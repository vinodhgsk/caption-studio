<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Standard Electron macOS entitlements.
  Required for Hardened Runtime + notarisation.
  Fill in additional entitlements (e.g. com.apple.security.device.camera)
  before a production release if the app uses those capabilities.
-->
<plist version="1.0">
  <dict>
    <!-- Allow JIT compilation (required by V8 / Chromium). -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (required by Electron renderer). -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow DYLD environment variables (required by some native modules). -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation so third-party native Node addons load. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Outgoing network connections (for OneDrive sync, update checks, etc.). -->
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
