---
name: community-animation-agent
description: Owns Doc 17. Seventeen community-reference kinetic text animations (wave-ripple, glitch-split, typewriter-caret, kinetic-3d, scramble-decode, gloss-sweep, mask-line-rise, elastic-word-pop, focus-blur-in, char-drop-tumble, shimmer-gradient, jelly-squash, neon-flicker, liquid-fill, particle-assemble, perspective-slam, variable-weight-wave) composed from the animation + reveal primitives.
tools: Read, Write, Edit, Bash, Glob, Grep
model: inherit
---
You implement community-reference text animations (docs/17): seventeen named presets inspired by external CodePen demos, each registered under a `community/` namespace in the existing animation/reveal registries. Implement each against its detailed recipe in docs/COMMUNITY_ANIMATION_RECIPES.md (per-glyph math, params with defaults, reference CSS, Canvas/WebGL mapping). Compose existing primitives wherever possible — the keyframe-engine evaluator (Doc 06), the reveal-effects engine (Doc 15), text effects (Doc 04), and gradient fill (Doc 10) — and add new mechanics only where needed (kinetic-3d/perspective-slam per-letter 3D transforms; scramble-decode random-character resolve; focus-blur-in letterSpacing relayout; shimmer-gradient animated fill; jelly-squash non-uniform scale; neon-flicker seeded flicker envelope; liquid-fill wavy alpha-mask; particle-assemble particle system; variable-weight-wave variable-font axes). All per-letter work operates on grapheme clusters (indic-text) so Tamil/Indic text animates as single units. Persist selections through the standard `clips[].animation` slots; render live thumbnails. The per-pen specs are best-guesses to be confirmed against the source URLs in the doc; keep everything deterministic (seed any randomness) for headless export parity. Done when each preset renders, respects timing, composes with effects/decorations, and matches on export.
