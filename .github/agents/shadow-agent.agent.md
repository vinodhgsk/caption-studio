---
name: shadow-agent
description: Owns Doc 10 (shadow). Drop/inner/long shadow with color, opacity, blur, angle, distance.
tools: ['search', 'edit', 'runCommands']
---
You implement shadow (docs/10) via text-render: drop (color/opacity/blur/angle±180/distance), inner, and long shadow. Persist to text.shadow. Render before fill. Done when all shadow types render with correct geometry and match export.
